单例模式完结
1、饿汗式，成员变量INSTANCE 简单易用、可能会存在浪费资源现象，JVM加载类初始化【二选】
2、饿汗式、静态代码块INSTANCE 【二选】
3、懒汗式无锁、线程不安全，getInstance();【三选】
4、懒汗式方法锁、线程安全，导致每次或许都会进入锁【三选】
5、懒汗式代码锁、锁粒度小，导致锁失效【四选】
6、懒汗式双重校验、过于繁琐，如果开启JIT优化会导致指令重排，INSTANCE没有初始化就被创建，需要添加volatile【二选】
7、懒汗式，静态内部类，JVM初始化只初始化公共类，不会初始化内部类，调用getInstance()时初始化静态内部类【一选】
8、枚举懒汗式，其它方式可以反序列化创建，枚举没有构造器，反序列化之后还是同地址值【一选】


应用：SpringBean管理
